const fs = require('fs'); const path = require('path'); module.exports = function writeFile(relPath, content) { const fullPath = path.join(__dirname, '..', relPath); const dir = path.dirname(fullPath); if (!fs.existsSync(dir)) fs.mkdirSync(dir, { recursive: true }); fs.writeFileSync(fullPath, content, 'utf8'); console.log('Written ' + relPath); };